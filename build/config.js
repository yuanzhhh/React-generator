const path = require('path');

const SRC_PATH = path.resolve(__dirname, '..', 'src');

module.exports = {
    // src
    src: SRC_PATH,

    // 入口文件
    entryPath: '',

    // 开发模式下配置
    dev: {
        
    },

    // 生产模式下配置
    build: {

    },
}